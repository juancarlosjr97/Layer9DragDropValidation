<?php


                  if ($data[$c] == 'invoicenumber')
                               {
                                   $col = 'invoicenumber';
                                   include 'conditions/invoicenumber.php';
                               }

                  if ($data[$c] == 'servicedate')
                               {
                                   // $col = 'servicedate';
                                   // include 'conditions/servicedate.php';
                               }


                  if ($data[$c] == 'financialperiod')
                               {
                                   // include 'conditions/financialperiod.php';
                               }

                  if ($data[$c] == 'custid')
                               {
                                   // include 'conditions/custid.php';
                               }

                  if ($data[$c] == 'custname')
                               {
                                   // include 'conditions/custname.php';
                               }

                  if ($data[$c] == 'siteid')
                               {
                                   // include 'conditions/siteid.php';
                               }

                  if ($data[$c] == 'sitename')
                               {
                               //     include 'conditions/sitename.php';
                               }

                  if ($data[$c] == 'addr1')
                               {
                                   // include 'conditions/addr1.php';
                               }

                  if ($data[$c] == 'addr2')
                               {
                                   // include 'conditions/addr2.php';
                               }

                  if ($data[$c] == 'city')
                               {
                                   // include 'conditions/city.php';
                               }


                  if ($data[$c] == 'postcode')
                               {
                                   // include 'conditions/postcode.php';
                               }

                  if ($data[$c] == 'wastetype')
                               {
                                   // include 'conditions/wastetype.php';
                               }

                  if ($data[$c] == 'descr')
                               {
                                   // include 'conditions/descr.php';
                               }

                  if ($data[$c] == 'wastedescription')
                               {
                                   // include 'conditions/wastedescription.php';
                               }

                  if ($data[$c] == 'ewccode')
                               {
                                   // include 'conditions/ewccode.php';
                               }

                  if ($data[$c] == 'disposalmode')
                               {
                                   // include 'conditions/disposalmode.php';
                               }

                  if ($data[$c] == 'disposalmodedesc')
                               {
                                   // include 'conditions/disposalmodedesc.php';
                               }

                  if ($data[$c] == 'qty')
                               {
                                   // include 'conditions/qty.php';
                               }


                  if ($data[$c] == 'kgs')
                               {
                                   // include 'conditions/kgs.php';
                               }

                  if ($data[$c] == 'tonnes')
                               {
                                   // include 'conditions/tonnes.php';
                               }

                  if ($data[$c] == 'costs')
                               {
                                   // include 'conditions/costs.php';
                               }
?>
